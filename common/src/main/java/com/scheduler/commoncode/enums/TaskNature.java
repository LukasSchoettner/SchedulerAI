package com.scheduler.commoncode.enums;

public enum TaskNature {
    FIXED_ESTIMATE,  // a clear, realistic estimated duration
    OPEN_ENDED       // progress is measured by incremental improvement
}

